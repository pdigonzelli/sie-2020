FIND PALLET WHERE PALLET.ID_PALLET = -1.


CATCH EX AS PROGRESS.LANG.ERROR.
    UNDO, THROW NEW PROGRESS.LANG.APPERROR('ERROR PROVOCADO').
/*    RETURN ERROR NEW PROGRESS.LANG.APPERROR('ERROR PROVOCADO'). */
END.
